// Testimonial schema - separate file for better organization
// Re-export from services schema for now, but can be moved to own file later
export {
  TestimonialSchema,
  type Testimonial,
} from '../services/services.schema';
